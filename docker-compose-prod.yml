services:
  geodispatch-app:
    container_name: geodispatch-prod
    environment:
      APP_ENV: production
      APP_DEBUG: false
    ports:
      - "80:8000"   # في البروداكشن غالبًا تستخدم port 80
